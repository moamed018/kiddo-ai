// import { useSettings } from "../context/SettingsContext";

export default function Logo() {
    // const settings = useSettings();

    // // console.log(settings);

    return (
        <h2 className="text-3xl uppercase font-bold tracking-wide select-none text-main-text">
            <b className="px-2 bg-main text-main-bg mr-0.5">k</b>
            iddo
        </h2>
    );
}
